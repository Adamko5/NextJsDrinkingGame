/**
 * ESM Next.js config. Next.js does not support `next.config.ts` in many
 * setups, so use `.mjs` (or `.js`) instead. Keep the same options here.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
};

export default nextConfig;
